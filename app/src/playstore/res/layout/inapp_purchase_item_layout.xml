<?xml version="1.0" encoding="utf-8"?>
<merge
	xmlns:android="http://schemas.android.com/apk/res/android"
	tools:layout_width="match_parent"
	tools:layout_height="wrap_content"
	xmlns:tools="http://schemas.android.com/tools"
	tools:cardCornerRadius="2dp"
	tools:contentPadding="8dp"
	tools:layout_margin="16dp"
	tools:elevation="4dp"
	tools:parentTag="androidx.cardview.widget.CardView">

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:orientation="vertical">

		<TextView
			android:id="@+id/title_view"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:padding="8dp"
			android:textAppearance="@style/Text.Dark.Medium.Larger"
			tools:text="Standard"/>

		<TextView
			android:id="@+id/subtitle_view"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:paddingStart="8dp"
			android:paddingEnd="8dp"
			android:textAppearance="@style/Text.Dark.Large"
			tools:text="Buy this in-app package to support the app and get us a coffee."/>

		<TextView
			android:id="@+id/bought_view"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:visibility="gone"
			tools:visibility="visible"
			android:layout_marginTop="4dp"
			android:paddingStart="8dp"
			android:paddingEnd="8dp"
			android:textAppearance="@style/Text.Dark.Medium"
			android:text="@string/donate_already_purchased_text"/>

		<Button
			android:id="@+id/purchase_cta"
			style="@style/Widget.AppCompat.Button.Colored"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_marginStart="4dp"
			android:layout_marginTop="8dp"
			android:textAllCaps="false"
			android:textSize="14sp"
			tools:text="Support with $0.99"/>

		<Button
			android:id="@+id/brag_cta"
			style="@style/Widget.AppCompat.Button.Colored"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:textSize="14sp"
			android:visibility="gone"
			android:layout_marginStart="4dp"
			android:layout_marginTop="8dp"
			tools:visibility="visible"
			android:text="@string/donate_card_brag_cta"/>

	</LinearLayout>

</merge>